# 领域定义重写验收

基线：`27a9ed11c5f7230c8bca2060d3b3022dcd2c981f`。Unity：6000.3.24f1。日期：2026-09-20。

本文记录该次迁移的历史验收。原始精确对照及其 JSON 已归档至 `Tools/RetiredMigrations/20260920/`，退出日常总回归；当前内容允许正常新增与调参。后续入口和规则见 [验证工作流](<验证工作流.md>)，不可把本文中的固定数量和值继续作为正式资产的日常要求。

## 交付边界

- 删除统一 Definition / Catalog / Rule、全局定义索引与通用实体工厂；22 个领域各自拥有资产、编译器、Blob、ID、查询和显示目录。
- 22 个 DefinitionAsset 直接声明领域字段，删除对应的整份 DefinitionSource 与 Data 包装；保留 Objectives、Rewards、Prerequisites 等语义组合。
- 将会话、设置、建筑运行状态分成明确组件；建筑、经济、库存、军事、任务和朝廷行为由领域服务拥有实现。
- 通用命令载荷改为独立请求组件，队列只持有路由与顺序信息；按队列所有权在消费、清空或成功替换时释放请求实体，恢复回滚保留原队列及其载荷。
- 存档使用 v30 的领域记录与明确状态，类型化索引由配置指纹约束；不兼容旧版存档。
- UI 的导航、会话、选择、远征和情报等状态有独立归属，切换世界时释放旧绑定和实例。

作者入口和扩展方法见[定义系统](../定义系统.md)。

## 验证

删除整份 DefinitionSource / Data 包装后，`Landsong/ECS/Verification/Run all` 于 2026-09-20 13:26:24 完成：**50 组，13,925 条断言，0 失败**。覆盖真实资产编译、地图制作及烘焙、玩法、事务、存档、UI 和架构边界。架构验证明确禁止22个顶层 Source 类型和 Asset.Data 字段或兼容属性，并检查编译器直接接收对应 Asset。

真实 Odin Inspector 绘制于 13:26:43 通过，展开后的领域字段和嵌套模块正常显示；全量回归同时验证直接字段的中文标签、引用编辑及 Undo。

185 个资产仅移除外层 Data 并提升其字段。独立迁移审计核对 1,314 个根字段、11,221 个叶值及全部引用与顺序；历史基线文件字节不变，8,310 个历史字段通过仅移除顶层 Data. 前缀的测试路径映射继续核对。此映射只属于历史基线测试，生产代码没有兼容入口。细节与前后快照位于 `Library/LandsongEcs/FlattenDefinitions`。

本轮 Windows Development 构建于 13:28:49 完成：**Succeeded，0 错误，5 条警告**，用时 1 分 32 秒。输出为 `Builds/ECSBaseline/Landsong.exe`。

新独立运行包通过 `-landsong-ecs-smoke` 重新执行完整流程，于 13:30:32 输出 **PASS** 并以退出码 **0** 结束，覆盖 Boot、Start、Loading、Game 四场景及真实 UI、战斗、事务恢复和退出清理。本轮结果保存在 `Library/LandsongEcs/FlattenDefinitions/player-result.json`。

本轮原始证据保存在 `Library/LandsongEcs/FlattenDefinitions`：`regressions.txt`、`content-inspector-gui.txt`、`build.txt` 及 Player 日志与结果。元数据扫描没有缺失、孤立的 `.meta` 或重复 GUID；`git diff --check` 通过。

迁移基线验证保留 185 项定义的 GUID、fileID、配置值、领域内顺序和 408 个跨域引用。独立编译器另校验运行默认值和任务数量缩放。所有旧万能类型的缺席、22 种 ID 的类型隔离及独立目录接口均有自动检查。

本次发现并修复的行为问题包括：研究确认指纹的领域区分、任务数量在 Blob 构建阶段的缩放、相同排序值的夜战奖励去重、英雄和士兵招募失败的身份序号恢复、零字段请求的 ECS 访问、跨世界 UI 缓存和肖像实例释放，以及领域 ID 的唯一字符串表示。UI 验证实际渲染两个科技条目并检查重排后的对象身份与点击目标。

## 既有资源情况

市场和陆上远征所占位几何体原有 7 处悬空材质引用已改为项目现有的 `Building.mat`；模型和有效美术材质保持原样。

王宫的可选图标在基线中已指向不存在的资源。本次保留该原始引用；迁移测试同时检查 Unity 中仍为空、目标 GUID 仍不存在、原始 GUID/fileID 未被改变。

正式发布继续要求完整肖像素材并关闭 `PortraitConfig.Placeholders`，开发构建可使用现有占位素材。

构建的 5 条警告来自已有的过时 API：验证用 `TwcLabConnection` 的 3 处 NavMeshLink 调用，以及 Easy Save 3 的 2 处 `Light.cookieSize` 调用。独立程序退出后仍记录 ComputeBuffer 垃圾回收释放提示；2026-09-18 的既有 Player 日志中也有同一提示。本次未改动这些 API 或渲染资源生命周期，也未将烟测通过描述为无告警。
