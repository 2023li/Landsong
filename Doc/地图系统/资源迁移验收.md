# GameMaps 资源与验收

地图制作流程见 [地图制作](README.md)，最新执行结果见 [问题修复与验收](../ECS/问题修复记录20260917.md)。

## 当前资源

- 地图资源集中到 `Assets/Landsong/GameMaps`；源场景位于各地图外层，TWC 数据位于 `<场景名>Data/Source`，烘焙配置、逻辑网格、MapAsset 和实体子场景位于 `Generated`。
- 游戏菜单仅保留 Map_Map01；公共 TWC 模板及 Map_NavigationLab、Map_TWCFlowLab 专项实验资源继续保留。业务验证入口通过 VerificationMap 统一指定地图。
- 测试在 Map_Map01 的隔离 World 或临时复制地图中执行；不会重新生成旧地图，也不修改玩家存档。
- 实体场景使用 GUID 引用；正常烘焙保留身份并同步菜单与 Game 宿主。固定出生区域已经由动态入场和 TWC 边缘区取代，见 [动态刷怪](动态刷怪方案.md)。
- TWC 编辑器协程通过受限的完成等待入口，确保网格与后处理完成后再导出；已有交互生成未完成时拒绝启动另一轮烘焙。

## 验收入口

运行 `GameMapWorkflowVerification` 检查初始化、复制隔离、烘焙、发布与失败回滚；`ArchitectureVerification` 检查保留地图的来源/烘焙一致性；地图加载 Play 检查当前菜单地图及重复进入后的身份和释放。完整业务流程另由 `EcsPlayerSmoke` 验证。

报告位于 `Library/LandsongEcs/`，以本次完成时间和最终 PASS/FAIL 为准；不能使用删除地图之前的加载报告证明当前版本通过。
