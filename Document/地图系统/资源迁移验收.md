# GameMaps 资源迁移验收

本次执行日期：2026-09-14。地图制作流程见 [地图制作](README.md)。

## 完成内容

- 地图资源集中到 `Assets/Landsong/GameMaps`，源场景在各地图外层，TWC 数据保存在 `<场景名>Data/Source`，烘焙配置、逻辑网格、MapAsset 和实体子场景位于 `Generated`。
- 保留用户新增的公共 TWC 模板、Map_Map01 草稿和菜单顺序。Map_Test01 的独立地形规则保留原来的陆地语义。
- 新增初始化、校验和完整烘焙入口；源场景负责初始建筑、出生区域、弹体阻挡及开局参数。生成文件不再作为手工配置入口。
- 实体场景使用 GUID 引用，正常更新保留资产身份；烘焙自动同步菜单及 Game 宿主。
- `Objects/SO/UIConfig.asset` 移至 `Objects/Prefabs/UI/Bootstrap/UIConfig.asset`；建筑命名配置移至 `Art/BuildingViewNamingConfig.asset`。两份资产内容和 GUID 均未改变，旧 SO 目录及空分类目录已删除。
- TWC 编辑器协程增加受限的完成等待入口，确保所有地形网格生成及后处理完成后再导出；已有交互生成尚未完成时拒绝启动另一轮烘焙。升级插件时需保留此适配。

## 本次验证结果

| 验证 | 结果 |
| --- | --- |
| C# 编译 | 0 错误；有既有测试夹具未赋值字段警告 |
| 地图制作专项 | 18 项通过：空白场景初始化、重复初始化、复制隔离、首次烘焙、真实网格保存、菜单增删、无效建筑保护、发布失败回滚、完整 Map_Test2 逐格一致 |
| 地图加载专项 Play | 9 项通过；完成 Map_Test2、Map_Test01、再次 Map_Test2 的加载、身份/单模拟根检查和回菜单释放 |
| 全领域 Run all | 42 个套件通过，1 个 UI 配置套件失败；通过套件共 4760 项断言。不能标记全项目回归通过 |
| 四场景完整 Play | 在 Start 的唯一活动 AudioListener 检查失败，尚未完成完整流程。地图加载专项不能替代此结果 |
| 资源审计 | 15 个原地图资源 GUID 均保留且唯一；两张既有地图的 TWC 绘图数据保留 |

UI 配置套件的 12 个问题涉及既有 GamePanel 本地化、音效、肖像引用、预览标记和运行时组件查找；相应 UI 预制体及运行脚本本次未修改。完整 Play 的音频监听器重复也位于既有场景/UI 根。本轮没有扩展修改这些 UI/音频问题，没有构建独立程序。

完整日志在 `Library/LandsongEcs/`：`map-authoring-verification.txt`、`map-loading-play-verification.txt`、`regressions.txt`、`ui-configuration-verification.txt`、`scene-flow-verification.txt`。迁移前资源备份在同目录的 `GameMapsMigrationBackup/`，一次性迁移脚本保留在 Library 中，不作为日常项目工具。
