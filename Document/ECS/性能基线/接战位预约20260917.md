# 接战位空间预约检查与采样

2026-09-17 16:18:08（北京时间），在本机 Unity 6000.3.24f1 Editor 执行 `TacticalReservationVerification.Run()`。374 条断言通过，包含负坐标、建筑周界与单位环形候选点、0.35 / 1.2 / 3.8 的混合碰撞半径、逆序创建后按稳定 ID 处理，以及其他会话单位排除。

测试用完整遍历此前所有预约的独立参照计算期望位置，逐单位比较空间表给出的 `HasSlot` 与坐标。此检查用于证明优化保留预约结果，不通过只检查容器类型来判断正确性。

| 单位数 | 更新 p50 | 当前线程托管分配 / 次 | 获得预约的单位数 |
| ---: | ---: | ---: | ---: |
| 20 | 0.327 ms | 0 B | 13 |
| 50 | 1.285 ms | 0 B | 31 |
| 100 | 2.519 ms | 0 B | 53 |
| 200 | 4.939 ms | 0 B | 106 |

每个规模使用独立 World，先预热 5 次，再计时 25 次。计时包含生产路径的实体查询、稳定排序、原生空间表创建与释放，不包含夹具创建和参照检查。不运行 Job、渲染、存档或正式地图。托管分配来自 `GC.GetAllocatedBytesForCurrentThread`；原生 Temp 内存仍然存在。

这些数字是当前实现的 Editor 微基准，没有旧实现耗时对照，不用于声称提升倍率、整帧性能或 Player 达标。实际战斗仍需结合调用频率、感知 Job 等待和目标设备帧预算分析。

复现：退出 Play、等编译完成后执行 **Landsong → ECS → Verification → Tactical reservations and timings**。也可在 Unity 已关闭时执行 `-batchmode -quit -executeMethod Landsong.ECS.Editor.TacticalReservationVerification.Batch`。本机原始报告为 `Library/LandsongEcs/tactical-reservation-verification.txt`，日志为 `report-fixes-tactical.log`；以当次完成时间为准。
