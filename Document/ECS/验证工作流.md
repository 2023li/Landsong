# 验证工作流

本页前半部分是当前验证入口。后半部分保存重构前的历史执行记录，不能作为本轮 v24 协议、共享 UI、程序集和资源迁移的验收结论；本轮实际结果以 [重构执行记录](../重构执行记录.md) 及对应时间的报告为准。

## 日常怎么确认通过

1. 保存要保留的场景修改并退出 Play，等待脚本编译与资源导入完成。验证期间不要并行修改源码或启动第二个 Unity 实例。
2. 执行 Unity 菜单 **Landsong → ECS → Verification → Run all**。
3. 打开 `Library/LandsongEcs/regressions.txt`。最后一行必须为 PASS，failures=0，且完成时间属于本次执行。文件不存在、只写 Started、出现 FAIL 或编译失败，都不能算通过。
4. 涉及真实界面/场景/战斗时，再执行 **Landsong → ECS → Verify four-scene flow (Play)**。结束后查看 `Library/LandsongEcs/scene-flow-verification.txt` 的本次 PASS。测试自动退出自己的 Play 并恢复原场景。
5. 涉及打包或运行差异时，执行 **Landsong → ECS → Build Windows baseline**，查看 build.txt 并运行新的独立程序。

这些领域回归是项目自定义断言，与 Visual Studio / Unity Test Runner 里的绿色勾选列表不是同一个入口。GridMapDefinitionTests 是保留的 NUnit 测试；不要用它的绿色勾选代替整个项目验证。

建筑详情 / 研究 HUD / 功能面板调整可运行 **Landsong → ECS → Verify research HUD and panels (Play)**，本次结果写入 hud-panels-verification.txt（带完成时间）。该定向测试包含新版建筑详情的默认展开、输入改名、异常悬浮、补贴预算/黄色即时预览/实际已付效果保留/比例岗位/空基础产出隐藏/工人档位悬浮侧栏、作物选择/进度/铲除、缺蓝图升级原因、升级材料确认/取消/执行、MAX、坐标与默认范围；同时覆盖默认隐藏、各功能关闭、驻军双视图、情报退出、科技队列进度/效果/空态、完整科技树交互，以及王室分代/家庭/登基连线/死亡/皇冠、右侧人物详情/父母/操作门禁/王朝事务切换、赐婚事件及双方详情弹窗、同意/拒绝/稍后处理、主动候选选择与确认、人物多请求列表/搁置后重开/请求数量标记/导航不结单/逐项处理/空态/关闭门禁，以及肖像缓存共享/丽质 RGB 与部件预览/取消和一次确认/读档重新绑定/程序衰老/幼儿襁褓/士兵列表；测试结束恢复原场景。截图输出 building-details.png、royal-family.png、marriage-request.png、arrange-marriage.png、person-requests.png 与 royal-requests-details.png。Court 领域回归额外验证生育资格、女性继位、父母性别映射、主动赐婚、候选稳定、换君、拒婚风险、请求去重/过期/阶段限制和快照往返。它不等于完整四场景/战斗验收。仅调整 UI 时优先编辑器验证，涉及独立运行差异才额外构建。

## 代码与输出

- Scripts/Editor/Tests/Verification：领域套件、ProjectVerification 总入口、真实 Play 调度；套件数量以当前总入口为准。
- Scripts/Verification/Tests：编辑器和 Development Player 共用完整流程，正式 Release 不包含 smoke 行为。
- Scripts/Editor/Tests/GridMapDefinitionTests.cs：独立 NUnit 网格检查。
- 每个领域写自己的 `<领域>-verification.txt`；总报告为 regressions.txt，不嵌套重复调用其他套件。
- 只跑 UI 快速复现输出 interface-ui-verification.txt，不能代替完整场景报告。
- 测试使用独立 GUID 临时目录及非持久会话，不操作玩家王朝；借用的设置、设备、字体/资源测试对象在 finally 归还。
- Library/LandsongEcs 是可再生本机证据，不提交版本库。先检查本次时间与结果，不能仅凭旧文件存在判定。

PersistenceContractVerification 检查 v24 显式字段覆盖、全局设置与网格签名、未登记根组件拒绝、采集失败回滚，以及两份重构前真实 v23/v3 金样本的读取、恢复和 v24 再保存。ArchiveApplicationServiceVerification 检查 Load/Save 权限、跨王朝拒绝、自动节点保护、失效会话和相同长度/修改时间下的内容变更。它们已并入总入口，输出分别为 persistence-contract-verification.txt 和 archive-application-service-verification.txt。

ArchitectureVerification 校验八个项目程序集分区、脚本归属及依赖方向，包括 Authoring 与第三方 AI 适配从运行核心分离；UiConfigurationVerification 校验实际根画布、注册表、显式引用、脚本/资源目录、中文标签与预览配置。GamePanelNavigationVerification 检查类型化面板标识、迁移后九个原有静态导航事件的目标和参数、配置权限与显示身份分离。上述静态检查不代替真实 Play 中的输入、异步加载、失效会话与清理检查。

完整场景 smoke 会记录常驻 Canvas、UIManager、EventSystem 的初始实例，在 Start 与 Game 实际打开设置和存档后比较共享面板实例，并在退出 Game 后检查旧面板销毁、会话释放、示例内容清理。下拉框关闭后才统计实际活动 Canvas，避免把 TMP 临时展开画布当成第二套应用画布。这些检查是否实际通过仍以本次 Play 报告为准。

历史/库存验证通过正式 HistoryOps.ForCommand 或 GameLoopSystem.Execute 建立命令上下文。StorePending 与 StorePendingSlot 的待存放扣减、仓库增加必须标记为转库且数量平衡；中文原因改写不改变经济统计，其他命令也不能仅因文案包含“待存放转库”而获得转库标记。

## 命令行

Unity 已关闭时，可用对应版本的 Unity.exe：

```powershell
& 'C:/Program Files/Unity/Hub/Editor/6000.3.5f2/Editor/Unity.exe' -batchmode -quit -projectPath 'D:/UnityProject/Landsong' -executeMethod Landsong.ECS.Editor.ProjectVerification.Batch -logFile 'D:/UnityProject/Landsong/Library/LandsongEcs/batch.log'
```

Batch 先打开 Boot，运行全回归，再分别构建 Development（Builds/ECSBaseline）和 Release（Builds/ECSRelease）。只做逻辑回归可改用 ProjectVerification.ValidateBatch。构建必须 Succeeded 且错误数为 0；退出码不能替代报告检查。不与同项目已打开的 Unity 并发启动。

独立程序：

```powershell
& 'D:/UnityProject/Landsong/Builds/ECSBaseline/Landsong.exe' -landsong-ecs-smoke -screen-width 1600 -screen-height 900 -screen-fullscreen 0 -logFile 'D:/UnityProject/Landsong/Library/LandsongEcs/player.log'
```

需要 Development Build 和显式 -landsong-ecs-smoke。完整成功会输出 [ECS PLAYER SMOKE] PASS 并返回 0；异常、超时和流程未跑完不算通过。输入/画面测试不要使用 -nographics。

## 本机编辑器自动化

EcsEditorAutomation 读取 Library/LandsongEcs/request.json，每个唯一 Id 只处理一次，结果写 response.json。只在非编译/非更新状态执行；Refresh 返回不代表编译已完成。

常用请求包括 InspectEditor、InspectScenes、Refresh、Validate、VerifyAll、VerifyPersistenceContracts、VerifyUiConfiguration、VerifyUiFramework、VerifyContentInspector、VerifyPortraitImport、OpenPortraitImport、VerifyArchitecture、SceneFlowPlay、SceneFlowInterfacePlay、SceneFlowHudPanelsPlay、SceneFlowGarrisonPlay、StopSceneFlowTest、Build、BuildRelease、SyncPresentationText。具体可用操作以 EcsEditorAutomation 的 switch 为准，一次性资源迁移/安装动作不是日常验证入口，完成重构后不要为解决普通配置问题重复执行。

VerifyContentInspector 用临时资产打开实际 Odin 检查器，绘制结果写 content-inspector-gui.txt；只写 Started 或 INCOMPLETE 不算通过，窗口画面还需可视核对。VerifyPortraitImport 单独验证肖像导入服务和窗口门禁；其错误报告保留 Validate 的真实拒绝原因。如果临时资产操作触发待处理源码编译，必须待编译稳定后重新执行，不能移除导入器的 Play/编译保护来使测试通过。OpenPortraitImport 打开导入窗口。Stop 只允许停止调度器拥有的测试，不停止用户 Play。完成后移除请求文件。

## 重构前的历史验证记录

### 独立功能面板（2026-09-11）

经济、库存、驻军、情报和战报已迁移为独立组件及场景窗口。HUD 定向 Play 于 **14:36:31** 通过，新增检查每个功能独占内容容器、窗口互斥、驻军两列同属一个父对象、关闭后保持隐藏，并保留全部研究/王室/赐婚/请求流程。完整四场景 Play 于 **14:38:24** 通过，覆盖实际库存拖放/拆分、驻军招募/分配、情报只读模式、历史战报与回合确认、暂停和恢复。领域回归于 **14:39:08** 通过：27 套件、3769 条断言、0 失败。已检查本次 function-panel-inventory.png；仅编辑器验证，未构建。

首次定向测试发现加载阶段先隐藏 UI 时，库存拖放清理早于组件绑定；已修复未绑定状态的清理，并通过上述完整复验。窗口拆分的一次性编辑脚本已移除，不保留运行时自动迁移或旧共享窗口回退。

### Game 场景 UI 整理（2026-09-11）

领域回归于 **14:12:57** 通过：27 个套件、3769 条断言、0 失败。HUD 定向 Play 于 **14:04:39** 通过，检查新的父层绑定、默认显隐和既有功能；固定建筑操作栏的最终完整四场景 Play 于 **14:22:11** 通过，包含建筑详情父子关系、内联改名、放置提示开始/取消、驻军、科技、任务、暂停、英雄与存档流程。已查看本次 building-details.png，确认操作栏位于详情底部、滚动内容不被遮挡、下一阶段按钮可见。本次仅进行编辑器验证，未构建。

层级重组曾暴露英雄测试仍从 Canvas 直接查找 Battle HUD 的旧定位；已改为从 HudRoot 查找，并通过原有英雄行为断言。最后复验前保存了 Game 场景，测试结束恢复场景。布局及脚本职责见 [Game 场景布局](../UI与输入/Game场景布局.md)。

原生内容校验及 27 组领域回归全部通过，合计 **3769 条断言**；完成时间为 2026-09-11 12:07:39。内容编译套件为 379 条，覆盖全部 185 个定义及开局奖励的迁移前后逐项对照、执行顺序、资产往返与夜间建筑等级/完工条件；Odin 检查器套件为 22 条。建筑模块套件包含 82 条检查，覆盖 29 个建筑的有效规则对照、资产序列化、无效配置拒绝、初始驻军配置，以及作物/采集奖励的实际等级执行。PortraitImportVerification 为 46 条，覆盖原始 PNG 的 32/64 尺寸、单前发/单后发导入与合成、身体多层、单层服装、窗口按需增删层、空部件/重复层拦截、男女复选、标识及目录冲突、错误输入禁止提交、源文件原样复制、Sprite 导入设置、配置注册、占位与美术共存，以及复制/校验/注册阶段失败回滚。测试使用临时资产，未修改实际肖像配置；本轮资源窗口调整仅进行编辑器验证，没有构建。

PortraitVerification 现为 52 条断言，覆盖 32/64 像素合成、透明叠加、年龄/颜色缓存、性别部件、丽质无特性/年龄/暂停/夜晚/过期草稿/重复提交门禁、基础颜色遗传、快照往返/错误拒绝/事务回滚、士兵自然死亡换人和战死纪念；以及此前建筑补贴、王室/赐婚、远征请求与全部既有领域检查。断言合并了同一子代循环中的重复统计，只保留一次集合结果，不以重复断言增加功能覆盖数。

编辑器 HUD 定向 Play 于 **2026-09-10 15:27:26** 通过（hud-panels-verification.txt），包含任务两列展开/收起、真实皇宫任务来源、签约移列、放弃取消、逐项提交、完成变色/对勾领奖，以及肖像缓存共享、丽质部件/RGB 草稿、取消与一次确认、读档重新绑定、程序衰老、幼儿襁褓、士兵列表图片与不裁切边界，同时保留建筑、研究、面板、家谱、赐婚和请求流程。已查看实际截图确认布局，任务截图为 quests-mainline.png、quests-completed.png、quests-quests-Map_Test2.png；保留 portraits-family.png、beauty-customization.png、portrait-swaddle.png、soldier-portraits.png，均在 Library/LandsongEcs。本轮仅使用编辑器，没有构建；定向流程不替代完整四场景战斗验收。

最近一次 Windows Development 构建来自此前的主菜单调整（2026-09-09，0 错误 / 2 警告 / 85.14 秒），对应 1280×720 独立程序完整流程 PASS，日志 main-menu-player.log。它不包含随后新增的研究 HUD/功能面板、王室/赐婚和肖像改动。此前编辑器暂停 Esc 检查曾失败；2026-09-10 14:34:11 的通用界面定向 Play 已重新通过真实 Esc、暂停/恢复及存档界面检查。Release 构建仍为 2026-09-08 基准（0 错误 / 1 警告 / 80.96 秒），见 build-release.txt、build-boundaries.txt。

断言数包含参数与地图重复检查，不等于独立功能数量。退出资源诊断仍见 [当前限制](当前限制.md)，功能通过不表示性能或内存释放全面验收。

任务两列与容器绑定回归：完成待领奖占槽、实际建筑/槽号绑定、重复槽位存档拒绝、停工/缺工/维护失败/缩容/拆除时丢失任务（含待领奖）、一次性弃任务处罚，以及入夜事务失败恢复任务和处罚。当前 HUD 定向 Play 同时运行 QuestUi，覆盖原地展开/收起、右侧签约移入左侧、X 取消放弃、逐项提交、绿色完成卡与 ✓ 确认领奖。

本轮语言同步为 1430 条；最终 HUD 定向 Play 与截图核对通过。仅在 Unity 编辑器验证，没有构建测试。

皇宫 LV1 增加 3 个通用驻兵槽后，24 组编辑器领域回归通过（3225 条断言）。普通驻地荒废与夜间改道测试明确排除核心；入夜事务测试使用超出实际容量的槽号构造溢出，避免假定皇宫没有驻兵槽。本次配置调整未新增构建验证。

任务 HUD / Toggle 调整的领域回归覆盖：物品奖励倍率只计算一次、非物品奖励不折算、价值降序、直接后续优先于更高价值无关任务、无后续时回退最高价值已接受任务、领奖失败不改追踪、领取其他任务不抢追踪、明确取消和自动目标读档保持。

追踪取消命令携带所属任务 ID，在命令执行时校验当前目标；验证快速勾选后取消遵守最终输入，以及迟到的其他任务取消命令不会清掉新目标。语言同步为 1430 条。

任务 HUD / Toggle 的最终编辑器 Play 于 2026-09-10 11:59:17 通过：真实 UGUI 勾选/取消/连续切换、其他卡同步取消勾选、分类多选及全关、实际邀约价值排序、HUD 直接领奖并自动续接。已检查 quest-invitation-toggles.png 和 quest-hud-reward.png，位于 Library/LandsongEcs；本轮未构建。

## 建筑模块迁移及此前待验内容（2026-09-10）

用户已说明没有并行任务，并要求一并验证此前待验内容。本节结果覆盖之前“本轮不验证”的建筑驻军、开局两名守军、敏捷属性、通用任务槽和分步衔接调整；没有进行构建。

- `SoldierVerification` 74 条通过：待分配募兵、付款/人口、指定槽位分配等。
- `BuildingModuleVerification` 69 条通过：29 个建筑迁移前后有效配置对照与序列化往返；开局驻军超容量、错误引用类型、残留旧 Rules、等级越界拒绝；多组初始兵种与模块禁用；作物和最终采集奖励按建筑等级执行。
- `SceneFlowGarrisonPlay` 于 14:03:26 通过，报告 `garrison-ui-verification.txt`：皇宫开局两名驻军及读档不重复、基础产出在主容器、空槽打开管理、募兵确认进入待分配池、选择后分配到指定槽、已有槽打开士兵详情、改名、敏捷/血量显示和关闭门禁。已查看 `building-garrison.png`、`soldier-details.png`。
- `SceneFlowHudPanelsPlay` 于 14:04:27 通过，报告 `hud-panels-verification.txt`，包含本页所述建筑、科研、王室、请求、肖像与任务流程。
- `SceneFlowInterfacePlay` 于 14:34:11 通过，报告 `interface-ui-verification.txt`：暂停 Esc、家谱节点与滚动保持、历史/返回栈、输入焦点、键鼠及模拟触屏镜头操作、设置/改键，以及独立存档创建、覆盖取消、重命名、预览、加载确认和删除。
- `SceneFlowPlay` 于 **14:43:36** 完整通过，报告 `scene-flow-verification.txt`：Boot → Start → LoadingTransition → Game 的 Map_Test2 流程；包含上述界面、王室/人才/政策、士兵巡逻召回、英雄、实际 DBP 战斗、情报、平安互动、任务/邀约/远征、科技、建筑/岗位、库存、经济预测与入夜结算确认，以及快照与完整王朝归档恢复、重复加载、损坏存档/错误地图/取消后的清理。旧版王室行列表与“没有肖像对象”的断言已更新为家谱节点、右侧详情和稳定人物 ID 肖像绑定；手工生成人物的表现测试也补齐正常肖像初始化。

任务通用容量与分步衔接检查已并入本次 25 组领域回归，先前 4 项失败没有继续搁置。测试夹具同步了皇宫自带两名士兵、任务真实承接容量及新版王室/驻军的操作入口；未通过跳过断言来标记成功。


## 驻军分组、士兵卡片与特别关注（2026-09-10）

25 组领域回归于 15:17:10 通过，共 3355 条断言。PortraitVerification 新增改名不关注、取消关注不触发、关注保存/恢复、v22 命名标记迁移为未关注、无效 Toggle 值拒绝，并保留自然死亡换人清除关注及战死只发一次遗言检查。

HUD 综合 Play 于 15:27:26 通过，包含新版驻军用例：按建筑共同父对象、待分配选中不弹详情、独立详情按钮、关注 Toggle、分配后右栏移除、移出/重新分配、等级相同但经验不同按稳定 ID 排序、总属性排序不受当前伤势影响、姓名文字实际渲染、建筑工具条不遮挡、立即遣散释放真实人口和槽位。同时回归原建筑详情、王室/请求/肖像、任务与科技面板。实际截图为 `Library/LandsongEcs/garrison-cards.png`，已检查两栏卡片的文字和按钮布局。

本轮仅运行编辑器验证，没有构建。

完整四场景 Play 于 15:24:45 通过，包含新版分组驻军、真实招募/移出/补位、详情改名、巡逻和召回，以及王室、战斗、存档和场景清理。最终 15:27:26 的 HUD 复验额外确认按卡片实际生命上限排序，并验证同等级不同总属性会正确换序。

## 建筑工人档位与补贴显示验证（2026-09-10）

本轮 VerifyAll 于 16:07:00 完成，25 套件、3355 项断言全部通过。最终 SceneFlowHudPanelsPlay 于 16:08:29 通过：无产出农田隐藏整个基础产出块、切换仓库恢复该块；预算增加立即显示黄色预览且不扣款，预算减少清除预览而保留当前已付效果；种植悬浮显示工人数档位、停长条件和全周期奖励，查看不修改快照或随机状态；侧栏可移入滚动、离开关闭，仓储切换无旧作物内容，关闭详情清除侧栏。已检查 worker-tiers.png 与 subsidy-preview.png，标题避开导航按钮、黄色增量可见。全部在编辑器内完成，无构建。

## 显式工作效率档位验证（2026-09-10）

最终 VerifyAll 于 16:19:23 通过：25 套件、3366 项断言。新增覆盖档位缺失、重叠、断档、越界、跨效果门槛的拒绝，以及显式 0～1 人区间、资产序列化、单向编译保留配置、烘焙查询按配置排序、未配置等级不推导、仅改分组保持快照与玩法内容签名不变。现有 13 个岗位建筑已填写人数档位，全部内容校验通过。

SceneFlowHudPanelsPlay 于 16:20:31 通过，工人侧栏使用 WorkerEfficiencyOps 的明确档位查询结果，已复查 worker-tiers.png。测试恢复 Boot 场景，本轮仅编辑器验证，无构建。


## 建筑驻军肖像修复验证（2026-09-10）

SceneFlowGarrisonPlay 于 17:27:54 通过。新增检查建筑驻军槽具有实际可见 Sprite、保持比例且不拦截点击，关闭再打开驻军块后复用槽位并恢复肖像；已有招募、分配、移出、排序、关注、详情与遣散流程继续通过。已查看 building-garrison-portraits.png，确认建筑详情中显示士兵肖像及姓名。相关说明已同步，未运行构建。

## Odin 内容检查器与代码清理（2026-09-10）

VerifyAll 于 17:57:06 完成，26 套件、3382 条断言全部通过。ContentInspectorVerification 的 16 条检查覆盖实际 OdinEditor 入口、Odin 属性树解析中文标签、所有嵌套配置字段的标签、只读检查不修改序列化内容，以及内容引用修改、撤销和错误类型拒绝。原生资产校验、建筑编译对照、开局驻军、工作效率档位及既有领域回归均通过。反向迁移器已删除，相关测试直接构造强类型模块，不再依赖旧规则迁移。

当天实际检查器可视检查被 Windows 交互桌面阻挡，GUI 探针未获得有效重绘；Started 或黑屏未被计为通过。后续收尾见下节。上述 Play 时间保留各次实际验证结果。


## 全内容强类型配置收尾（2026-09-11）

`RuleSource`、原始 Rules 字段、EffectiveRules、ConvertRule 和旧迁移器均已删除。185 个内容定义、开局发放及夜间条件均采用具名配置；建筑与非建筑模块直接生成运行记录，引用统一解析，校验与 Blob 写入共用编译结果。迁移对照保留原有数值、引用索引、顺序和任务标识，快照仍为 v23、王朝归档仍为 v3。

最终 VerifyAll 于 **12:07:39** 完成，27 套件、3769 条断言全部通过。新增检查确认夜间建筑最低等级和完工状态真实影响资格；同时覆盖非法引用、跨模块顺序和 Unity 序列化往返。建筑数值控件改用 Odin MinValue，实际编译前校验同步读取下限；整数正下限和浮点负值拒绝用例均通过。仅检查标签属性不足以证明绘制正确，实际窗口核对另外记录。

交互桌面恢复后，已在真实 Unity Inspector 展开农田的移动设置、建筑模块及岗位条目，并抽查农业科技的功能模块和蓝图奖励。确认“废墟通行消耗”、岗位容量/吸引力/费用、模块标题、执行顺序、建筑引用和许可等级等字段显示中文，资产引用正常显示内容名称和 ID。本次可视检查直接查看原生窗口，没有把旧 GUI 探针的 Started 报告改记为 PASS，也没有编辑资产数值。此前待验的完整四场景与各 UI Play 结果见前面的实际完成记录；本轮没有构建。
