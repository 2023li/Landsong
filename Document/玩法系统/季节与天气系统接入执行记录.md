# 季节与天气系统接入执行记录

日期：2026-09-23。规则依据：[季节、天气与风设计草案](季节与天气系统设计草案.md)。本文件按章节记录实际接入、验证结果和未完成项；只有完成实现并完成本节验证后，才将状态改为「完成」。

## 执行顺序与记录

| 节 | 范围 | 状态 | 完成记录 |
| --- | --- | --- | --- |
| 1 | 基线与接入边界：核对现有 ECS、内容资产、存档、表现层及工作区改动 | 完成 | 已核对根实体、黎明入口、农业结算、加权范围、主光控制和快照结构；当前快照为 v34。 |
| 2 | 季节、温度、天气、风的权威状态及每日抽取 | 完成 | 地图烘焙和 200 次黎明推进通过；季节边界、气温界限与雨雪条件通过。 |
| 3 | 作物生长值及跨季节结算、农田 UI | 完成 | 实际农田结算验证冬季 3 回合得 1.5、入春后连续得 2.7/3.0；UI 与成熟外观共用新门槛。 |
| 4 | 雪天道路代价、人物寻路与建筑服务范围 | 完成 | 实际道路节点代价为晴天的 1.3 倍；建筑加权可达范围相应缩小。 |
| 5 | 白天打雷计时、相机内落雷、目标/奖励分支 | 完成 | 相机外闪屏不计数、可见地面/建筑落雷、单位幸存蓝图/重复金币/死亡分支通过。 |
| 6 | 消防站、消防员、火灾状态与阶段期限、雷神殿占位建筑 | 完成 | 新建筑资产与建筑学奖励烘焙通过；自动派员、阶段延期、抵达灭火、逾期荒废及消防员死亡结算通过。 |
| 7 | 四季滤镜、雨雪、风摆、雷击火焰占位表现与 HUD | 完成 | 四套 URP Profile、雨雪粒子激活及现有树木强风摆动/无风复位通过；HUD、光照与雷击/火焰占位接入并完成 Unity 编译。 |
| 8 | 存档/读档、回归验证与整体验收 | 完成 | v35 天气/火灾/消防员快照恢复通过；季节天气 245 项、存档契约 241 项、文档 228 项校验通过；季节天气套件已注册 Unity Test Runner，定向运行 1 通过、0 失败。 |
| 9 | 调试面板与 F8 入口 | 完成 | 独立常驻 DebugManager 监听 F8；天气按钮仅在游戏世界就绪时可用，非游戏世界定向 Test Runner 用例 1 通过、0 失败。 |

## 记录规范

每节完成时填写：改动文件或资产、对应规则、实际运行或静态验证、已知限制。若验证不通过，保持「进行中」并记下问题与处理结果。首次接入期间不覆盖已有的未提交改动，不把既有改动记为本功能成果。

## 第 1 节：基线与接入边界

现有根实体由 `GameWorldTemplateAuthoring` 创建，回合 1 在 `NightOps.Dawn` 自增。当前白天没有持续增加的 `GameClock.Time`；天气与打雷需要独立计时。农业进度目前是整型回合数；雪天道路须同时接入 A* 和 `BuildingRangeOps`。主光由 `NightLightingController` 管理。当前工作区的 `SnapshotCodec.CurrentVersion` 已是 **34**，且农业、UI、存档等文件有其他未提交改动；这些改动均保留。验证：静态读取并定位上述入口，未改动玩法代码。

## 第 2 节：季节、温度、天气、风

新增 `SeasonWeatherState`、`SeasonWeatherSettings` 和 `SeasonWeatherOps`；新游戏回合 1 生成初始日状态，黎明按回合推进。天气专属随机状态与经济随机流分离，保存当天温度、天气、风、雷击上限和下一候选时点。参数由世界模板 Authoring 提供。快照协议在现有 v34 基础上增至 v35，保留旧版本读取路径。**完成验证**：实际地图烘焙通过；新游戏为春季第 1 回合、8°C；连续推进 200 次黎明，逐日检查季节、当季气温界限和雪天低于 0°C；四季 30/20/30/20 边界单独检查。随机分布为策划可调参数，未做统计显著性验收。

## 第 3 节：作物生长值

作物进度以十分之一为权威单位，春/夏秋/冬每次有效结算分别增加 12/10/5；成熟门槛为原 `GrowthTurns × 10`，跨季节累计不折算。农田 UI 显示百分比、1 位小数进度和当前季节预计回合；作物成熟外观也使用新门槛。v34 及以前的读档进度乘 10 迁移。**完成验证**：实际农田连续在冬季结算 3 次得到 1.5/3.0，转春后 2 次得到 2.7/3.0 与 3.0/3.0；另以 10 回合作物验证冬季 10 次累积半程、转春预计剩余 5 回合。

## 第 4 节：雪天道路代价

新增共用的 `RoadWeatherCostOps.Context`，只对道路类别（含荒废与归类为道路的坡道）在雪天应用 1.3 倍；`SurfaceNavigationGraph` 与 `BuildingRangeOps` 使用同一有效代价。雪天状态变化时增加网格版本并使导航图失效。**完成验证**：实际地图上导航道路节点代价为晴天的 1.3 倍，同一消防站的建筑加权服务距离增加、覆盖相应缩小。具体覆盖边界由地图道路布局决定。

## 第 5 节：打雷与落雷

雨天白天按随机 60～120 秒持续尝试；70% 纯闪屏，30% 进入落雷，达到当天 1～3 次上限后仍可纯闪屏。落雷地面与目标各 50%，目标类在建筑、可见友方单位间均分并按约定回退；建筑只选正常运营且非核心建筑，画面无有效地面时降为闪屏。幸存单位血量降至 1，首次给雷神殿蓝图，再次给待存放金币。**完成验证**：构造实际相机视锥，画面外只闪屏且不计数，画面内地面与建筑分支分别命中；固定随机种子检查 1% 幸存给蓝图、重复幸存给 1000 金币（含待存放）、死亡扣消防员岗位和人口。70/30 与 50/50 是按实现条件和确定性分支验证，未做大样本概率统计。

## 第 6 节：消防、火灾与雷神殿

火灾状态使建筑暂停运营、暂不可取库存，并在白天/夜晚阶段边界结算。消防员由在岗工人名额派出，按雪天同一加权道路范围选最近合格消防站，到达后灭火，返回后释放名额；被击杀时扣岗位及人口。消防站与雷神殿为现有占位方块预制体，消防站参数为 2×2、2 回合、20 金币+10 木材+10 石材、4 岗、行动力 30、消防员速度 1.6，雷神殿为 2×2、1 回合、最多 1 座、无功能；建筑学奖励消防站蓝图。**完成验证**：Unity 完整目录编译、地图烘焙及新建筑资产校验通过；起火暂停运营、消防站自动派员、在途跨黄昏继续救援、抵达灭火并回程、无人救援到下一主要阶段进入可修复荒废、消防员被雷击死亡减少岗位及人口均通过。敌军现场战斗与多站接替未进行可视化游玩验收。

## 第 7 节：表现与 HUD

新增 `WeatherPresentationController`：四季分别建立占位 URP 后处理 Profile；雨雪粒子、闪屏、落雷线条均使用运行时占位资源。雨天主光亮度为晴天的 75%，雪天沿用晴天亮度；季节 Profile 在黎明直接切换。`WorldPresentationView` 根据每日风向和风力摆动现有 ECS 树木的已烘焙可见槽，无风恢复原旋转；起火建筑显示橙色球体与点光占位。HUD 显示季节日序、天气、温度、风力和火灾救援期限。

**加载场景生命周期修复（2026-09-23）**：`BindLighting` 执行时活动场景仍是稍后卸载的 Loading 场景，原先动态创建的天气根对象因此随 Loading 被销毁，而 Game 场景中的 `EcsGameHost` 继续调用粒子对象，产生 `MissingReferenceException`。现在创建后立即将天气根对象移入相机所属的 Game 场景。编辑器回归用例明确把相机放在与活动场景不同的场景，并检查天气根对象的场景归属。

**完成验证**：Unity 编译通过；编辑器用例创建四季状态，确认四套 Profile 不共用同一实例、雨雪分别启用对应粒子；实际 `b树木1` 建筑生成后选中烘焙模型槽，强风旋转发生变化，无风回到原旋转。现有树木为组合网格，首版整棵树轻微摆动；雷声已在 `LandsongAudio.asset` 的 `Thunder` 提示中绑定项目现有 `Lighting (1).wav`，可在检查器中替换。占位画面尚未做人工审美验收。

## 第 8 节：存档与验收

`SnapshotCodec` 更新至 **v35**，保存季节天气及随机流、建筑火灾状态和消防员任务；`SnapshotValidation` 与 `RestoreTransaction` 同步补齐。v33/v34 保留字段读取与旧农业进度 ×10 的换算路径，但旧档仍须通过现有内容指纹校验，新增建筑导致不匹配的旧档不能直接加载。六份核心开发/存档文档同步标注 v35，文档首页增加本执行记录入口。

**完成验证**：编辑器针对实际地图烘焙后的新世界执行 `SeasonWeatherVerification`，**245 项通过**，包含每日状态、作物跨季、雪天导航与范围、相机落雷、单位奖励/死亡、消防出勤/期限、天气对象跨场景归属及 v35 快照恢复。`PersistenceContractVerification` **241 项通过**；`DocumentationVerification` **228 项通过**。季节天气套件已加入 `ProjectVerification.Suites`，由现有 NUnit `TestCaseSource` 自动呈现在 Unity Test Runner 的 EditMode 列表，名称为「季节与天气系统」；通过 Test Runner API 以「天气」分类定向运行，结果 **1 通过、0 失败**。首次 Test Runner 运行发现编辑器模式下雷击占位对象调用 `Destroy` 的错误，已改为现有 `Release` 清理路径后重跑通过。后续针对 Loading 场景卸载导致粒子对象被销毁的错误增加场景归属断言，再次重跑通过。一次性测试触发脚本及其 `.meta` 均已删除；范围内 `git diff --check` 无空白错误。验证报告保存在本机 `Library/LandsongEcs/`，编辑器菜单仍保留可重复运行的「Landsong → ECS → Verification → Season and weather integration」。

**首版待回填**：四季后处理颜色、雨雪/雷击/火焰和两座建筑模型均为占位；树木当前为组合网格，树干随整棵树轻微摆动。雷声暂用项目现有 `Lighting (1).wav`，可按美术需要替换。敌军现场攻击消防员、多站接替及整体视觉审美尚未进行人工游玩验收。工作区原有其他未提交改动均保留，未将其计入本功能的完成记录。

## 第 9 节：调试面板与 F8 入口

完成 `UI_DebugPanel` 与天气调试页，并将调试面板注册为应用作用域界面；独立的 `DebugManager` 在进入首个场景前创建并常驻，监听 F8，通过现有 `UIManager` 在菜单与游戏中开关调试面板。天气页可切换晴雨雪、调节温度和风力风向；雪天切换同步失效道路导航缓存。天气入口按 `GameReady`、有效 ECS 世界和天气状态刷新：非游戏世界直接禁用天气按钮，并收起天气页；进入游戏世界后自动恢复。预制体沿用项目的本地化文字和按钮点击音效绑定。

**完成验证**：改由独立 `DebugManager` 持有 F8 后 Unity 重新编译通过；新增 `DebugPanelEditModeTests` 在 Unity Test Runner 定向执行为 **1 通过、0 失败**，覆盖非游戏世界按钮禁用及无法打开天气页。完整 UI 配置验证中，本次面板的注册、命名、引用、本地化和音效检查通过；该验证仍因其他现存脚本的固定组件查找与检查器标签问题报告 10 项失败，报告见 `Library/LandsongEcs/ui-configuration-verification.txt`。F8 的实体键盘交互与天气参数在真实游戏画面中的人工体验尚未验收。
