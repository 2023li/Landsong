# 持久化协议金样本

这些文件由 **2026-09-11 重构前已经编译的 Unity 程序集**运行领域存档验证时产生，经文件监听器从 `Library/LandsongEcs/VerificationRuns/<GUID>/runs/.../state.lsrun` 捕获。它们不是使用 v24 写入器伪造的 v23 文件，也不是玩家存档。样本保持原始字节和封装 SHA-256。

| 文件 | 地图 | 格式 | 原始文件 SHA-256 |
| --- | --- | --- | --- |
| map-test01-day-v23.lsrun.bytes | Map_Test01 | 快照 v23 / 王朝 v3 | 7c44c941bfdbaceda41b46c0f8c4cb63a458b678e73e0b74522038ea08181094 |
| map-test2-day-v23.lsrun.bytes | Map_Test2 | 快照 v23 / 王朝 v3 | 6626ccaaa357b15caee5902c63fff888d673a7b659941821fc9a39daa9cd2d14 |

首次捕获的本机来源记录位于 `Library/LandsongEcs/RefactorBaseline-20260911-193405/GoldenV23`。原目录还包含故障测试制造的其他状态；本目录只选取两张地图的初始白天记录。

`PersistenceContractVerification.Run` 校验样本哈希、全部嵌入节点、原王朝归属、v23 恢复、冻结旧布局逐字节往返及 v24 再保存/恢复。它同时覆盖新版全部 GameSettings 字段的签名变化、网格原点和比例、未登记根组件、采集失败历史回滚及类型化历史类别。

## 兼容契约

- 新存档写快照 **v24**，王朝封装保持 **v3**。
- v24 按固定字段顺序显式编码，不按运行时结构体布局拷贝。
- v22/v23 仅经过对应的旧格式分支，使用冻结布局映射；v22 延续清空旧士兵命名标记的规则。没有“失败后猜另一种格式”的路径。
- 旧文件仍验证其原有内容签名。旧版没有记录的全局玩法设置无法追溯，不能假装能校验；加载不会改写文件，后续正常保存生成完整签名的 v24 节点。
- v3 王朝可以同时包含旧自动节点和新手动节点，每个节点按自己的明确版本校验，原有节点语义不变。
- 内容规则有意变化时，旧样本不可被重生成来消除失败；须审查真实兼容策略及独立的新内容基准。纯显示名称、菜单顺序和默认皮肤在 v24 的玩法签名中排除。

这些资产只用于编辑器验证，不位于 Resources，也不是运行期迁移工具。
